# AeroNet Lite

Python simulation: grid model, CSP layout validation, and matplotlib plots under `src/`. Run the layout demo with:

```bash
python src/main.py
```

Or open `main.ipynb` from this folder. See `plan.md` for phases and milestones.
